name: gpu_tf
channels:
  - pytorch
dependencies:
  - python=3.7
  - anaconda
  - pip
  - cudatoolkit=10.0
  - cudnn=7.6.0=cuda10.0_0
  - pytorch
  - torchvision
  - cpuonly
  - pip:
    - tensorflow-gpu
    - opencv-python
    - PySimpleGui
    - pydicom
